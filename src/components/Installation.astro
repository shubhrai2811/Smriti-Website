---

---

<section id="install" class="section">
  <div class="install-header reveal">
    <h2 class="section-title">
      Get Started in <span class="gradient-text">3 Steps</span>
    </h2>
    <p class="section-subtitle">From zero to intelligent memory in under a minute. Pick your IDE.</p>
  </div>

  <!-- IDE Tabs -->
  <div class="ide-tabs reveal">
    <button class="ide-tab active" data-tab="claude-code" type="button">
      <svg class="ide-tab-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="16 18 22 12 16 6" />
        <polyline points="8 6 2 12 8 18" />
      </svg>
      Claude Code
    </button>
    <button class="ide-tab" data-tab="cursor" type="button">
      <svg class="ide-tab-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z" />
      </svg>
      Cursor
    </button>
    <button class="ide-tab" data-tab="opencode" type="button">
      <svg class="ide-tab-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <rect x="2" y="3" width="20" height="14" rx="2" ry="2" />
        <line x1="8" y1="21" x2="16" y2="21" />
        <line x1="12" y1="17" x2="12" y2="21" />
      </svg>
      OpenCode
    </button>
  </div>

  <!-- Claude Code Panel -->
  <div class="tab-panel active" id="panel-claude-code">
    <div class="install-steps">
      <!-- Step 1 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">1</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Clone &amp; Build</h3>
          <p class="step-desc">Clone the repository and build with Bun.</p>
          <div class="code-block step-code">
            <button
              class="copy-btn"
              data-code="git clone https://github.com/shubhrai2811/Smriti.git
cd Smriti && bun install && bun run build"
              aria-label="Copy to clipboard"
            >
              <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2" />
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
              </svg>
              <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:none;">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </button>
            <pre><code><span class="line"><span class="comment">$ </span>git clone https://github.com/shubhrai2811/Smriti.git</span>
<span class="line"><span class="comment">$ </span>cd Smriti && bun install && bun run build</span></code></pre>
          </div>
        </div>
      </div>

      <!-- Step 2 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">2</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Register Hooks</h3>
          <p class="step-desc">Add Smriti to your Claude Code hooks configuration.</p>
          <div class="code-block step-code">
            <button
              class="copy-btn"
              data-code="# Add to your Claude Code hooks configuration
# See README for detailed hook setup"
              aria-label="Copy to clipboard"
            >
              <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2" />
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
              </svg>
              <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:none;">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </button>
            <pre><code><span class="line"><span class="comment"># Add to your Claude Code hooks configuration</span></span>
<span class="line"><span class="comment"># See README for detailed hook setup</span></span></code></pre>
          </div>
          <a href="https://github.com/shubhrai2811/Smriti#claude-code-hooks" class="step-link" target="_blank" rel="noopener noreferrer">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
              <polyline points="15 3 21 3 21 9" />
              <line x1="10" y1="14" x2="21" y2="3" />
            </svg>
            View hook setup guide
          </a>
        </div>
      </div>

      <!-- Step 3 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">3</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Start Coding</h3>
          <p class="step-desc">
            That's it. Smriti activates automatically on your next Claude Code session. Your AI will start remembering.
          </p>
          <div class="ready-badge">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
              <polyline points="22 4 12 14.01 9 11.01" />
            </svg>
            <span>Ready to go</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Cursor Panel -->
  <div class="tab-panel" id="panel-cursor">
    <div class="install-steps">
      <!-- Step 1 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">1</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Clone &amp; Build</h3>
          <p class="step-desc">Clone the repository and build with Bun.</p>
          <div class="code-block step-code">
            <button
              class="copy-btn"
              data-code="git clone https://github.com/shubhrai2811/Smriti.git
cd Smriti && bun install && bun run build"
              aria-label="Copy to clipboard"
            >
              <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2" />
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
              </svg>
              <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:none;">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </button>
            <pre><code><span class="line"><span class="comment">$ </span>git clone https://github.com/shubhrai2811/Smriti.git</span>
<span class="line"><span class="comment">$ </span>cd Smriti && bun install && bun run build</span></code></pre>
          </div>
        </div>
      </div>

      <!-- Step 2 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">2</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Configure Cursor</h3>
          <p class="step-desc">Add Smriti to your Cursor rules and copy the adapter config.</p>
          <div class="code-block step-code">
            <button
              class="copy-btn"
              data-code="# Add Smriti to your Cursor rules
# Copy the adapter config to .cursor/"
              aria-label="Copy to clipboard"
            >
              <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2" />
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
              </svg>
              <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:none;">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </button>
            <pre><code><span class="line"><span class="comment"># Add Smriti to your Cursor rules</span></span>
<span class="line"><span class="comment"># Copy the adapter config to .cursor/</span></span></code></pre>
          </div>
          <a href="https://github.com/shubhrai2811/Smriti#cursor-integration" class="step-link" target="_blank" rel="noopener noreferrer">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
              <polyline points="15 3 21 3 21 9" />
              <line x1="10" y1="14" x2="21" y2="3" />
            </svg>
            View Cursor setup guide
          </a>
        </div>
      </div>

      <!-- Step 3 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">3</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Start Coding</h3>
          <p class="step-desc">
            Memories sync across all your IDEs automatically. Use Cursor alongside Claude Code with the same shared memory.
          </p>
          <div class="ready-badge">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
              <polyline points="22 4 12 14.01 9 11.01" />
            </svg>
            <span>Memories sync across IDEs</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- OpenCode Panel -->
  <div class="tab-panel" id="panel-opencode">
    <div class="install-steps">
      <!-- Step 1 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">1</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Clone &amp; Build</h3>
          <p class="step-desc">Clone the repository and build with Bun.</p>
          <div class="code-block step-code">
            <button
              class="copy-btn"
              data-code="git clone https://github.com/shubhrai2811/Smriti.git
cd Smriti && bun install && bun run build"
              aria-label="Copy to clipboard"
            >
              <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2" />
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
              </svg>
              <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:none;">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </button>
            <pre><code><span class="line"><span class="comment">$ </span>git clone https://github.com/shubhrai2811/Smriti.git</span>
<span class="line"><span class="comment">$ </span>cd Smriti && bun install && bun run build</span></code></pre>
          </div>
        </div>
      </div>

      <!-- Step 2 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">2</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Configure OpenCode</h3>
          <p class="step-desc">Add Smriti hooks to your OpenCode configuration.</p>
          <div class="code-block step-code">
            <button
              class="copy-btn"
              data-code="# Add Smriti hooks to your OpenCode config
# See README for OpenCode integration"
              aria-label="Copy to clipboard"
            >
              <svg class="copy-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2" />
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
              </svg>
              <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:none;">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </button>
            <pre><code><span class="line"><span class="comment"># Add Smriti hooks to your OpenCode config</span></span>
<span class="line"><span class="comment"># See README for OpenCode integration</span></span></code></pre>
          </div>
          <a href="https://github.com/shubhrai2811/Smriti#opencode-integration" class="step-link" target="_blank" rel="noopener noreferrer">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
              <polyline points="15 3 21 3 21 9" />
              <line x1="10" y1="14" x2="21" y2="3" />
            </svg>
            View OpenCode setup guide
          </a>
        </div>
      </div>

      <!-- Step 3 -->
      <div class="install-step glass reveal">
        <div class="step-number-wrapper">
          <span class="step-number gradient-text">3</span>
        </div>
        <div class="step-content">
          <h3 class="step-title font-heading">Start Coding</h3>
          <p class="step-desc">
            All memories are stored in a shared SQLite database. Switch between IDEs without losing context.
          </p>
          <div class="ready-badge">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
              <polyline points="22 4 12 14.01 9 11.01" />
            </svg>
            <span>Shared memory database</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Tab switching
  document.querySelectorAll('.ide-tab').forEach((tab) => {
    tab.addEventListener('click', () => {
      const target = tab.getAttribute('data-tab');
      if (!target) return;

      // Deactivate all tabs
      document.querySelectorAll('.ide-tab').forEach((t) => t.classList.remove('active'));
      // Activate clicked tab
      tab.classList.add('active');

      // Hide all panels
      document.querySelectorAll('.tab-panel').forEach((panel) => {
        panel.classList.remove('active');
      });

      // Show target panel
      const targetPanel = document.getElementById(`panel-${target}`);
      if (targetPanel) {
        targetPanel.classList.add('active');

        // Trigger reveal for newly visible steps
        targetPanel.querySelectorAll('.reveal').forEach((el) => {
          el.classList.add('visible');
        });
      }
    });
  });

  // Copy to clipboard
  document.querySelectorAll('#install .copy-btn').forEach((btn) => {
    btn.addEventListener('click', () => {
      const code = btn.getAttribute('data-code') || '';
      navigator.clipboard.writeText(code).then(() => {
        const copyIcon = btn.querySelector('.copy-icon') as HTMLElement;
        const checkIcon = btn.querySelector('.check-icon') as HTMLElement;
        if (copyIcon && checkIcon) {
          copyIcon.style.display = 'none';
          checkIcon.style.display = 'block';
          setTimeout(() => {
            copyIcon.style.display = 'block';
            checkIcon.style.display = 'none';
          }, 2000);
        }
      });
    });
  });
</script>

<style>
  #install {
    text-align: center;
  }

  .install-header {
    margin-bottom: 2.5rem;
  }

  .install-header .section-subtitle {
    margin: 0.75rem auto 0;
  }

  .font-heading {
    font-family: var(--font-heading);
  }

  /* ===== IDE Tabs ===== */
  .ide-tabs {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 2.5rem;
    flex-wrap: wrap;
  }

  .ide-tab {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 1.5rem;
    border-radius: 9999px;
    font-family: var(--font-heading);
    font-size: 0.9375rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    background: rgba(8, 14, 30, 0.5);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: #94a3b8;
  }

  .ide-tab:hover {
    color: #e2e8f0;
    border-color: rgba(255, 255, 255, 0.15);
    background: rgba(8, 14, 30, 0.7);
  }

  .ide-tab.active {
    color: #ffffff;
    background: rgba(0, 217, 255, 0.08);
    border-color: transparent;
    background-image: linear-gradient(rgba(0, 217, 255, 0.08), rgba(139, 92, 246, 0.08)),
                      linear-gradient(135deg, var(--color-cyan), var(--color-purple), var(--color-magenta));
    background-origin: border-box;
    background-clip: padding-box, border-box;
    border: 1px solid transparent;
    box-shadow: 0 0 16px rgba(0, 217, 255, 0.15), 0 0 32px rgba(139, 92, 246, 0.08);
  }

  .ide-tab-icon {
    flex-shrink: 0;
    opacity: 0.7;
    transition: opacity 0.3s ease;
  }

  .ide-tab.active .ide-tab-icon {
    opacity: 1;
  }

  /* Light theme tabs */
  [data-theme="light"] .ide-tab {
    background: rgba(255, 255, 255, 0.6);
    border-color: rgba(0, 0, 0, 0.08);
    color: #64748b;
  }

  [data-theme="light"] .ide-tab:hover {
    color: #334155;
    border-color: rgba(0, 0, 0, 0.15);
    background: rgba(255, 255, 255, 0.8);
  }

  [data-theme="light"] .ide-tab.active {
    color: #0f172a;
    background-image: linear-gradient(rgba(14, 165, 233, 0.06), rgba(139, 92, 246, 0.06)),
                      linear-gradient(135deg, #0ea5e9, #8b5cf6, #ec4899);
    background-origin: border-box;
    background-clip: padding-box, border-box;
    border: 1px solid transparent;
    box-shadow: 0 0 16px rgba(14, 165, 233, 0.12), 0 0 32px rgba(139, 92, 246, 0.06);
  }

  /* ===== Tab Panels ===== */
  .tab-panel {
    display: none;
  }

  .tab-panel.active {
    display: block;
    animation: panelFadeIn 0.35s ease-out;
  }

  @keyframes panelFadeIn {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* ===== Install Steps ===== */
  .install-steps {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    max-width: 720px;
    margin: 0 auto;
  }

  .install-step {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
    padding: 2rem;
    text-align: left;
    transition: all 0.3s ease;
  }

  .install-step:hover {
    transform: translateY(-2px);
    border-color: rgba(0, 217, 255, 0.15);
  }

  [data-theme="light"] .install-step:hover {
    border-color: rgba(14, 165, 233, 0.2);
  }

  .step-number-wrapper {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    border-radius: 14px;
    background: rgba(0, 217, 255, 0.06);
    border: 1px solid rgba(0, 217, 255, 0.1);
  }

  [data-theme="light"] .step-number-wrapper {
    background: rgba(14, 165, 233, 0.06);
    border-color: rgba(14, 165, 233, 0.1);
  }

  .step-number {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    font-weight: 800;
    line-height: 1;
  }

  .step-content {
    flex: 1;
    min-width: 0;
  }

  .step-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: #f1f5f9;
  }

  [data-theme="light"] .step-title {
    color: #1e293b;
  }

  .step-desc {
    font-size: 0.9375rem;
    color: #94a3b8;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  [data-theme="light"] .step-desc {
    color: #64748b;
  }

  /* ===== Code Blocks ===== */
  .step-code {
    position: relative;
  }

  .step-code pre {
    margin: 0;
    white-space: pre-wrap;
    word-break: break-all;
  }

  .step-code code {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: #e2e8f0;
    line-height: 1.7;
  }

  [data-theme="light"] .step-code code {
    color: #334155;
  }

  .copy-btn {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.05);
    color: #94a3b8;
    cursor: pointer;
    transition: all 0.2s ease;
    padding: 0;
  }

  .copy-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    color: var(--color-cyan);
    border-color: rgba(0, 217, 255, 0.3);
  }

  [data-theme="light"] .copy-btn {
    border-color: rgba(0, 0, 0, 0.1);
    background: rgba(0, 0, 0, 0.03);
    color: #64748b;
  }

  [data-theme="light"] .copy-btn:hover {
    background: rgba(0, 0, 0, 0.06);
    color: var(--color-light-accent);
    border-color: rgba(14, 165, 233, 0.3);
  }

  /* ===== Step Link ===== */
  .step-link {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    margin-top: 0.75rem;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--color-cyan);
    text-decoration: none;
    transition: all 0.2s ease;
    opacity: 0.8;
  }

  .step-link:hover {
    opacity: 1;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  [data-theme="light"] .step-link {
    color: var(--color-light-accent);
  }

  /* ===== Ready Badge ===== */
  .ready-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 1.25rem;
    border-radius: 9999px;
    background: rgba(16, 185, 129, 0.1);
    border: 1px solid rgba(16, 185, 129, 0.2);
    color: #10b981;
    font-size: 0.875rem;
    font-weight: 500;
  }

  [data-theme="light"] .ready-badge {
    background: rgba(5, 150, 105, 0.08);
    border-color: rgba(5, 150, 105, 0.15);
    color: #059669;
  }

  /* ===== Responsive ===== */
  @media (max-width: 640px) {
    .install-step {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .step-code {
      text-align: left;
    }

    .ide-tabs {
      gap: 0.375rem;
    }

    .ide-tab {
      padding: 0.5rem 1rem;
      font-size: 0.8125rem;
    }
  }
</style>
