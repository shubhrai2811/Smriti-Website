---
---

<section id="features" class="relative">
  <div class="section">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <h2 class="reveal section-title">
        Why <span class="gradient-text">Smriti</span>?
      </h2>
      <p class="reveal section-subtitle mx-auto" style="transition-delay: 100ms;">
        Intelligent memory that understands what matters, not just what happened last.
      </p>
    </div>

    <!-- Features Grid -->
    <div class="features-grid">

      <!-- 1. Semantic Context Injection -->
      <div class="reveal glass feature-card glow-box-cyan" style="transition-delay: 0ms;">
        <div class="icon-container icon-cyan">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2a7 7 0 0 1 7 7c0 2.38-1.19 4.47-3 5.74V17a1 1 0 0 1-1 1H9a1 1 0 0 1-1-1v-2.26C6.19 13.47 5 11.38 5 9a7 7 0 0 1 7-7z"/>
            <path d="M9 21h6"/>
            <path d="M10 17v4"/>
            <path d="M14 17v4"/>
            <circle cx="12" cy="9" r="2"/>
            <path d="M12 7V5"/>
            <path d="M8.5 9H7"/>
            <path d="M17 9h-1.5"/>
          </svg>
        </div>
        <h3 class="feature-title">Semantic Context Injection</h3>
        <p class="feature-desc">
          Hybrid ranking with vector similarity, recency decay, importance weighting, and entity bonuses. Your AI gets the most relevant context, not just the most recent.
        </p>
      </div>

      <!-- 2. Reflection & Learning -->
      <div class="reveal glass feature-card glow-box-purple" style="transition-delay: 100ms;">
        <div class="icon-container icon-purple">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2L9 8.5 2 9.27l5 4.87L5.82 21 12 17.77 18.18 21 17 14.14l5-4.87L15 8.5z"/>
            <path d="M12 2v5"/>
            <circle cx="12" cy="12" r="1.5"/>
          </svg>
        </div>
        <h3 class="feature-title">Reflection & Learning</h3>
        <p class="feature-desc">
          Quick reflection after every session. Deep reflection identifies patterns across days. Builds an evolving developer profile that shapes future context.
        </p>
      </div>

      <!-- 3. Entity Graph -->
      <div class="reveal glass feature-card glow-box-cyan" style="transition-delay: 200ms;">
        <div class="icon-container icon-cyan">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="5" cy="5" r="2.5"/>
            <circle cx="19" cy="5" r="2.5"/>
            <circle cx="12" cy="19" r="2.5"/>
            <circle cx="12" cy="12" r="1.5"/>
            <line x1="7" y1="6" x2="10.5" y2="11"/>
            <line x1="17" y1="6" x2="13.5" y2="11"/>
            <line x1="12" y1="13.5" x2="12" y2="16.5"/>
          </svg>
        </div>
        <h3 class="feature-title">Entity Graph</h3>
        <p class="feature-desc">
          Tracks relationships between files, functions, and concepts. Detects hotspots — areas you work on frequently — and boosts their relevance automatically.
        </p>
      </div>

      <!-- 4. Cross-IDE Memory -->
      <div class="reveal glass feature-card glow-box-purple" style="transition-delay: 300ms;">
        <div class="icon-container icon-purple">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="3" width="20" height="14" rx="2" ry="2"/>
            <line x1="8" y1="21" x2="16" y2="21"/>
            <line x1="12" y1="17" x2="12" y2="21"/>
            <rect x="6" y="7" width="4" height="6" rx="1" fill="none"/>
            <rect x="14" y="7" width="4" height="6" rx="1" fill="none"/>
            <path d="M10 10h4" stroke-dasharray="1.5 1.5"/>
          </svg>
        </div>
        <h3 class="feature-title">Cross-IDE Memory</h3>
        <p class="feature-desc">
          Shared SQLite database works across Claude Code, Cursor, and OpenCode. Your memories follow you between tools.
        </p>
      </div>

      <!-- 5. Privacy-First -->
      <div class="reveal glass feature-card glow-box-cyan" style="transition-delay: 400ms;">
        <div class="icon-container icon-cyan">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2L3 7v6c0 5.25 3.75 10.14 9 11.25C17.25 23.14 21 18.25 21 13V7l-9-5z"/>
            <rect x="9" y="11" width="6" height="5" rx="1"/>
            <path d="M10 11V9a2 2 0 1 1 4 0v2"/>
            <circle cx="12" cy="13.5" r="0.75" fill="currentColor"/>
          </svg>
        </div>
        <h3 class="feature-title">Privacy-First</h3>
        <p class="feature-desc">
          100% local storage. Automatic secret redaction catches API keys, tokens, and passwords. No telemetry, no cloud sync. Localhost-only worker.
        </p>
      </div>

      <!-- 6. Observation Masking -->
      <div class="reveal glass feature-card glow-box-purple" style="transition-delay: 500ms;">
        <div class="icon-container icon-purple">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
            <circle cx="12" cy="12" r="3"/>
            <line x1="4" y1="4" x2="20" y2="20" stroke-width="1.5"/>
            <path d="M17 9l3-3"/>
            <path d="M7 15l-3 3"/>
          </svg>
        </div>
        <h3 class="feature-title">Observation Masking</h3>
        <p class="feature-desc">
          Older observations automatically reduce detail level, preserving key facts while trimming verbosity. Keeps your context budget efficient over time.
        </p>
      </div>

    </div>
  </div>
</section>

<style>
  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  @media (max-width: 1024px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .features-grid {
      grid-template-columns: 1fr;
    }
  }

  .feature-card {
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    border-color: rgba(255, 255, 255, 0.15);
  }

  [data-theme="light"] .feature-card:hover {
    border-color: rgba(0, 0, 0, 0.12);
  }

  .icon-container {
    width: 56px;
    height: 56px;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.25rem;
  }

  .icon-cyan {
    background: rgba(0, 217, 255, 0.12);
    color: var(--color-cyan);
  }

  .icon-purple {
    background: rgba(139, 92, 246, 0.12);
    color: var(--color-purple);
  }

  [data-theme="light"] .icon-cyan {
    background: rgba(14, 165, 233, 0.1);
    color: #0ea5e9;
  }

  [data-theme="light"] .icon-purple {
    background: rgba(139, 92, 246, 0.1);
  }

  .feature-title {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 1.2rem;
    margin-bottom: 0.75rem;
    color: #f1f5f9;
  }

  [data-theme="light"] .feature-title {
    color: #1e293b;
  }

  .feature-desc {
    font-size: 0.95rem;
    line-height: 1.7;
    color: #94a3b8;
  }

  [data-theme="light"] .feature-desc {
    color: #64748b;
  }
</style>
